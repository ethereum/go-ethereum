version: '3.8'

services:
  app:
    image: e1018mc/limechain:latest
    platform: linux/x86_64
    ports:
      - "8545:8545"
    environment:
      - ENV_VAR=Test
    networks:
      - devnet

networks:
  devnet:
    driver: bridge